# ── Secrets ──────────────────────────────────────────────────────────────────
docker-compose.yml
.env
*.env
env.example

# ── Large artifacts (trained models, datasets, eval outputs) ─────────────────
artifacts/
data/
# Run outputs are large and regenerated, so the tree is ignored ...
reports/**
# ... except the small metric artifacts that DEFINITIVE_NUMBERS.md cites. Every published
# figure must resolve to a file in this repository, which requires those files to be in it.
!reports/**/
!reports/latest/metrics.json
!reports/latest/latency.json
!reports/latest/metrics_test.json
!reports/latest/metrics_scifact_test.json
!reports/reference/metrics.json
!reports/reference/latency.json
!reports/latest_eval/ablations.csv
!reports/latest_eval/ltr_train_eval.json
!reports/latest_eval/shadow_ab.json
!reports/opensearch/metrics.json
!reports/opensearch/lexical_ablation.json
metaflow_card_cache/
deliverables/

# ── Runtime ──────────────────────────────────────────────────────────────────
dump.rdb
*.rdb
*.db

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.python-version

# ── Build / Lock ─────────────────────────────────────────────────────────────
uv.lock
*.egg-info/
dist/
build/

# ── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE ──────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── Backup files ─────────────────────────────────────────────────────────────
*.bak
*.bak.*
docker-compose.yml.bak*
src/app/main.py.bak*
src/app/deps.py.bak*
.streamlens_env/
*.dylib
*.so
.venv/
venv/
# Virtual environments
.streamlens_env/
.venv/
venv/
env/

# Large ML binaries - use git-lfs or exclude
artifacts/svd/user_embeddings.npy
data/raw/movielens/ml-latest/
data/raw/movielens/ml-latest.zip

# Python cache
__pycache__/
*.pyc
*.pyo
*.dylib
*.so

# OS
.DS_Store

# Garbage that should never be committed
.bak/
.minio/
.Rhistory
.metaflow/
.streamlens_env/
.venv/
.ruff_cache/
.pytest_cache/
metaflow_card_cache/
*.bkp
*.bin
xl.meta

# Generated artifacts (large, not code)
artifacts/posters_hq/
artifacts/sd_posters/*.png

# Keep schema and queries (they are code)
!schema.sql
!queries.sql

# Promotion pipeline: per-run outputs are regenerated daily and stay local;
# one curated evidence snapshot is tracked.
!reports/pipeline_evidence/**
