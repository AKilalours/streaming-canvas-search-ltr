apiVersion: 1

providers:
  - name: 'streaming-canvas'
    orgId: 1
    folder: 'Search LTR'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/dashboards
